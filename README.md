# TabEx
多标签页文件浏览器，类似 Clover 的 Windows 资源管理器增强工具

## 📥 下载

**最新版本**: 从 [Releases](../../releases/latest) 页面下载 `TabExplorer.exe`

下载即用，无需安装 Python 环境！

---

## ✨ 核心功能

- ✅ **多标签页浏览** - Chrome 风格标签页，独立历史记录，支持拖拽排序
- ✅ **高 DPI 自适应** - 自动检测屏幕 DPI 并动态缩放 UI，高分屏显示清晰
- ✅ **标签页固定** - 固定常用文件夹，开机自动恢复
- ✅ **TortoiseGit 集成** - 标题栏快捷按钮，一键打开 Log/Commit 窗口（需安装 TortoiseGit）
- ✅ **会话恢复** - 关闭时自动缓存标签，启动时智能恢复工作环境，支持窗口恢复提示
- ✅ **快捷导航** - 前进/后退、面包屑导航、智能路径补全、路径栏智能压缩
- ✅ **Everything 搜索** - 毫秒级全盘搜索，速度提升 100-1000 倍
- ✅ **书签管理** - 层级书签、导入/导出
- ✅ **轻量级提示** - 右下角 Toast 消息，5秒自动消失，无需点击确认
- ✅ **自动监听 Explorer** - 新打开的资源管理器窗口自动嵌入为标签页
- ✅ **误操作保护** - Ctrl+Shift+T 恢复关闭的标签页（最多 20 个）
- ✅ **文件系统监控** - 文件变化自动刷新（QFileSystemWatcher + 2s 轮询兜底），支持回收站恢复
- ✅ **智能状态栏** - 显示目录项目数、选中文件信息、总大小、修改时间等
- ✅ **全面拖拽支持** - 文件夹拖拽到标签栏、目录树任意位置打开
- ✅ **单实例模式** - 右键菜单打开文件夹时在已有窗口新建标签页
- ✅ **性能优化** - 大文件夹异步加载、分割器拖动节流、响应式布局

---

## 🆕 最近更新

### v3.5 (2026-03-26)
- **双击空白返回上级稳定性优化**：
  - 修复“有时成功、有时失败”的问题
  - 优化双击判定时对 `selected_before` 的处理，避免使用陈旧选中状态导致误拦截
  - 当 `current_selection=0` 且 hit-test 明确为空白时，不再因为旧选中状态跳过 `go_up`
  - 降低误判率，提升连续操作场景下的一致性

### v3.4 (2026-03-26)
- **设置窗口自适应高度**：
  - 取消固定高度，改为根据当前设置项自动计算窗口高度
  - 保持固定宽度，避免布局抖动
  - 增加屏幕高度上限保护，防止超出可视区域
- **内存释放功能下线**：
  - 删除“每天凌晨自动重启一次（释放内存）”功能
  - 设置界面移除对应选项
  - 清理相关配置项与计时器逻辑，简化配置与启动流程

### v3.3 (2026-03-26)
- **每日重启改进**：
  - 修复凌晨自动重启后窗口无法显示的问题
  - 使用 cmd 延迟机制确保新旧进程不竞争端口和文件
  - 新进程启动延迟改为 ~2 秒（通过 ping 127.0.0.1 -n 3），旧进程完全释放资源后才启动
- **任务栏交互改进**：
  - 修复任务栏二次点击不最小化的问题
  - 主窗口现在保留标准窗口能力（WindowSystemMenuHint、WindowMinMaxButtonsHint）
  - 支持标准的任务栏切换行为：一次点击前置/恢复，再次点击最小化
  - 兼容 WM_SYSCOMMAND 的 SC_MINIMIZE 和 SC_RESTORE 命令
- **Bug 修复**：
  - 修复 QSplitter setCollapsible 在子控件添加前调用导致的 Qt 警告
  - 将 setCollapsible 调用移到 addWidget 之后，确保正确的执行顺序

### v3.2 (2026-03-19)
- **面包屑导航栏性能优化**：
  - 导航时立即刷新面包屑显示，消除约 1 秒的延迟
  - 实现智能节流机制：导航直接更新 vs 窗口 resize 节流
  - 从 75ms 缩短到 50ms 响应时间，用户体验显著提升
- **菜单栏溢出按钮优化**：
  - 修复收藏夹">> "按钮难以点击的问题
  - 菜单栏区域的右边缘拖拽功能已禁用，不再干扰菜单项点击
- **弹出菜单超出边界修复**：
  - 修复收藏夹菜单超出软件边界时无法点击进入目录的问题
  - 优化边框调整检测逻辑，当鼠标在窗口边界外时不再拦截菜单点击事件
  - 菜单超出窗口任意方向都能正常交互

### v3.1 (2026-03-17)
- **设置界面布局优化**：
  - 窗口高度增加至 750px，容纳更多设置项
  - 窗口宽度增加至 750px，检查更新链接完整显示
  - "检查更新"链接现在横跨左右两列，完整显示无截断
  - 设置项与快捷键设置分栏更清晰，避免重叠问题
- **Bug 修复**：
  - 修复首次运行时无 config.json 导致的崩溃问题（AttributeError: 'NoneType' object has no attribute 'get'）
  - Windows 11 25H2 兼容性验证通过

### v3.0 (2026-03-04)
- **新增快捷键**：
  - `Alt+Z` - 复制选中文件名（支持多文件，使用逗号分隔）
  - `Alt+X` - 复制文件路径\文件名（支持多文件，路径分隔符可在设置中配置）
- **路径分隔符统一**：路径拷贝时所有分隔符现在完全遵循设置中定义的分隔符，保持一致
- **快捷键设置**：在设置界面可单独启用/禁用 Alt+Z 和 Alt+X 快捷键

### v2.9 (2026-01-20)
- **设置界面全新两列布局**：常规设置与快捷键分栏，界面更清晰紧凑
- **路径栏分隔符设置即时生效**：更改分隔符后所有标签页立即刷新，无需重启
- **设置界面细节优化**：快捷键设置区域顶部与左侧第一个框对齐，底部与左侧第四个框底部对齐

### v2.8 (2026-01-15)
- **更新入口调整**：标题栏的“检查更新”按钮移至设置界面底部，以链接形式直达 GitHub Releases
- **书签拖拽需手动保存**：书签管理器拖拽排序后不再自动保存，点击“💾 保存”按钮手动确认
- **快捷键提示位置优化**：快捷键提示文字内嵌到快捷键设置分组，信息更聚合
- **视觉一致性**：设置中的更新链接背景改为与整体界面一致，保持纯净样式

### v2.7 (2026-01-14)
- **自动刷新增强**：双重机制确保文件更新及时显示
  - QFileSystemWatcher 实时监控文件系统变化
  - 2 秒轮询目录修改时间作为兜底，解决部分环境下监控事件丢失问题
  - 500ms 延迟刷新，避免频繁操作导致性能下降
- **智能状态栏**：Explorer 下方新增状态栏（参考系统资源管理器）
  - 未选中时：显示当前目录总项目数（如"15 项"）
  - 选中多个：显示"已选 N 项（共 T 项）"
  - 全选文件时：额外显示总文件大小（如"总大小 2.35 MB"）
  - 单选时：显示修改时间（如"修改时间 2026-01-14 15:30"）
  - 白色背景，12px 字体，顶部分隔线，视觉简洁
- **界面优化**：状态栏紧贴底部无留白，整体布局更紧凑

### v2.6 (2026-01-12)
- **TortoiseGit 集成**：标题栏新增快捷按钮（🐢 Log / 📤 Commit），仅在 Git 仓库中显示
- **非阻塞式轻量提示**：将所有阻塞式消息框替换为右下角自动消失的 Toast 提示（5秒）
- **交互体验优化**：
  - 提示消息显示在软件窗口右下角，多条消息自动向上堆叠
  - 无需点击确认按钮，可继续操作，不中断工作流
  - 支持多种提示级别（信息/警告/错误/成功），颜色区分一目了然
- **适用场景**：路径错误、搜索提示、TortoiseGit 操作反馈、书签管理等全部提示

### v2.5 (2026-01-07)
- **高 DPI 支持**：自动检测屏幕 DPI 并动态缩放 UI 元素（标题栏、标签栏、按钮、面包屑等），高分屏显示更清晰
- **路径栏智能压缩**：长路径自动显示中间省略号，保持目录树宽度稳定，拖动更顺畅
- **窗口恢复提示**：启动时在标题栏显示"窗口恢复中"，全部标签加载完成后自动清除
- **交互优化**：
  - 拖动分割器时节流更新，减少卡顿，交互更丝滑
  - 面包屑路径标签自适应宽度，带省略号显示
  - 路径栏响应式布局，可压缩至最小宽度
- **双击检测增强**：
  - 优化空白区域双击返回上级逻辑，500ms延迟确认机制
  - 多重信号验证（hit-test、选中状态、路径变化）防止误判
  - 双击文件夹进入更可靠，几乎杜绝意外返回上级的情况

### v2.4 (2026-01-06)
- **会话恢复功能**：新增标签页缓存，关闭时自动保存非固定标签，启动时智能恢复
- **窗口体验优化**：最大化状态下可直接拖动窗口，自动恢复大小并跟随鼠标，操作更流畅
- **界面优化**：导航按钮移至标题栏，窗口圆角效果，路径栏透明背景设计
- **路径栏改进**：优化字体显示（Segoe UI），修复复杂路径（如ZIP内路径）显示问题
- **目录树优化**：默认宽度增加至 500px，提供更宽阔的浏览视野
- **视觉改进**：路径栏与文件区分隔线，层次更清晰，视觉更舒适
- **性能优化**：返回上级目录速度提升，优化双击检测算法，窗口resize性能优化
- **稳定性提升**：双击文件夹进入更可靠，采用多重验证机制防止误判

### v2.3 (2026-01-04)
- **Everything 集成**：毫秒级搜索整个硬盘，速度提升 100-1000 倍
- **搜索优化**：搜索框宽度增至 300px，双击结果快速定位
- **性能提升**：大文件夹异步加载，百万级搜索结果支持
- **用户体验**：智能路径补全，调试模式开关

---

## 🛠️ 运行环境

**从可执行文件运行**（推荐）：
1. 从 [Releases](../../releases/latest) 下载 `TabExplorer.exe`
2. 双击运行即可

**从源码运行**（需要 Python 3.9+）：
1. 双击 `0_install_requirements.bat` 安装依赖
2. 双击 `1_TabEx.bat` 启动程序

**开机自启动**：
将 `1_TabEx.bat` 的快捷方式放到 `shell:startup` 目录

---

## 🔑 常用快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+T` | 新建标签页 |
| `Ctrl+W` | 关闭当前标签页 |
| `Ctrl+Shift+T` | 恢复关闭的标签页 |
| `Alt+←` / `Alt+→` | 后退 / 前进 |
| `Alt+↑` | 返回上级文件夹 |
| `Ctrl+F` | 搜索文件 |
| `Ctrl+D` | 添加当前路径到书签 |
| `F5` | 刷新当前文件夹 |
| `Alt+Z` | 复制选中文件名 |
| `Alt+X` | 复制文件路径\文件名 |

---

## 📖 主要功能说明

### Everything 搜索（推荐）
1. 下载安装 [Everything](https://www.voidtools.com/)
2. 下载 [es.exe](https://www.voidtools.com/downloads/) 并放到 Everything 安装目录
3. 启动 Everything 并等待索引完成
4. 在 TabExplorer 中按 `Ctrl+F` 打开搜索，勾选"使用 Everything"即可享受毫秒级搜索

### 自动监听 Explorer 窗口
- 类似 Clover 的核心功能
- 新打开的 Windows 资源管理器窗口自动嵌入为标签页
- 可在设置中启用/禁用

### 会话恢复（新）
- 关闭软件时自动保存所有非固定标签页
- 下次启动时自动恢复，继续上次的工作
- 恢复顺序：先加载固定标签，再恢复缓存标签
- 可在设置中启用/禁用（默认开启）

### 书签管理
- 支持文件夹层级组织
- 导入/导出 JSON 格式书签
- 兼容 Chrome 书签格式
  

### 标签页历史
- 最多记录 20 个关闭的标签页
- `Ctrl+Shift+T` 快速恢复
- 或点击标签栏右侧的 ↶ 按钮

### 文件复制快捷键
- **Alt+Z** - 快速复制选中文件名（如果选择多个文件，会用逗号分隔）
- **Alt+X** - 快速复制文件路径\文件名（路径分隔符可在设置中配置为 `/` 或 `\`）
- 两个快捷键都支持多文件批量拷贝
- 可在设置界面启用/禁用这两个快捷键

---

## 📝 更多文档

- [Everything 集成详细说明](EVERYTHING_INTEGRATION.md)

---

## 🐛 问题反馈

如遇问题，请在 [Issues](../../issues) 页面反馈，建议开启调试模式（`设置` → `启用调试输出`）以获取详细日志。




